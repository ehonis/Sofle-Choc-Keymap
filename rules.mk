# Sofle Choc build options for this keymap.
# encoder_map in keymap.c drives both knobs (volume only).
ENCODER_MAP_ENABLE = yes

# OLED “words per minute” readout (`get_current_wpm`) — weighted recent typing speed.
WPM_ENABLE = yes
